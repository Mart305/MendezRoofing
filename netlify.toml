[build]
  command = "npm install && CI=false npm run build"
  publish = "build"
  base = "/"

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
